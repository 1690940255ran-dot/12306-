# 定点起售抢票：时序验证报告

- 日期：2026-09-21 23:30–23:38
- 目标场景：乘车日 **2026-10-06**（实测**尚未开售**，与用户所述"明天 8:15 开售"一致）
- 目的：验证"提前打开软件 → 到点自动开抢（且快）"是否可靠
- 安全前提：目标日期未开售 → **不可能命中，全程未产生任何订单**

## 一、实测时序（压缩版：把开售时刻设到 23:37:00）

```
[23:30:06.783] 开始  目标开售时间 23:37:00
[23:30:06.784] 等待预热窗口（23:34:00 打开 12306 页面，剩余 4 分）
                       ——期间每 10 分钟自动保活登录，不访问查询接口
[23:34:00.000] 预热：打开官方查询结果页        ← 精确落在 T-3min（lead=180s）
[23:34:20.618] query_tickets -> 无数据（未开售，符合预期）
[23:34:20.619] arm_hit_watcher(安装页面观察器) ['G1']
[23:34:20.625] 页面已预热；23:37:00 到点查询，当前不刷新官方页面   ← 预热后不发刷新
[23:37:00.000] refresh_results #1（到点刷新）   ← 第一次刷新精确落在开售时刻
[23:37:03.151] refresh_results #2              ← +3.15s
[23:37:06.184] refresh_results #3              ← +3.03s
[23:38:01.314] 超过停止时间，结束抢票
[23:38:01.314] 结束 {"outcome": "no_ticket"}
总刷新次数: 20（60 秒内，间隔 3 秒）
```

## 二、逐条结论

| 验证点 | 结果 |
| --- | --- |
| 未开售期间**不访问**官方查询接口 | ✅ 23:30→23:34 纯本地等待（4 分钟零网络请求） |
| 预热窗口精确打开结果页 | ✅ 23:34:00.000（配置 T-3min，毫秒级对齐） |
| 预热后不浪费请求（不提前刷新） | ✅ 明确"当前不刷新官方页面" |
| **第一次刷新精确落在开售时刻** | ✅ **23:37:00.000**，误差 < 1 毫秒 |
| 到点后快速轮询 | ✅ 3 秒间隔，60 秒内 20 次刷新 |
| 未开售时正确判定"无数据"而非误判有票 | ✅ 结果页无车次行 → 保持占位，不误报 |
| 到停止时间正常收尾 | ✅ `no_ticket` |

**"等到时间就自动开抢，且快"这一条，实测成立。**

## 三、说明与边界

- 本次为**纯时序验证**：脚本把 `check_booking_login` 替换为恒真以跳过登录门控
  （登录门控当天已用真实扫码 + 真实抢票验证过），其余全部走**真实浏览器 + 真实 12306 页面**。
- 目标日期未开售，因此**不可能命中下单**，全程零订单。
- **"命中后自动下单"** 已于 2026-09-21 用真实车次验证（C432 南京→江都，官方订单已生成），
  见 `docs/2026-09-21-真实抢票验收与修复.md`。
- 轮询间隔下限 2 秒（配置项 `rush_interval_seconds`，2~30）；过高频率会触发 12306 风控，反而毁掉抢票窗口。

## 四、明天 8:15 实操清单

1. **提前登录**：`railassist --data-dir .runtime\prod --remember login`，本人扫码；
2. **保持在线**：`railassist --data-dir .runtime\prod --remember keepalive`
   （实测可让会话持续 5 小时不掉，见 `docs/2026-09-21-登录保活.md`）；
3. **建任务**（抢票模式）：
   - 乘车日填 **2026-10-06**（只能一个日期、一个席别）；
   - 车次白名单填目标车次（可多个，按优先顺序）；
   - 勾选**抢票模式 + 自动提交**；
   - **开售时间必须手填** `2026-09-22T08:15:00+08:00`
     —— 注意：`sale-time fetch` 当前恒返回 null（已知问题），请以官方公告为准；
4. **登记授权**：订单页 `order authorize --task <ID> --passengers <真实姓名> --max-amount <上限分> --confirm`；
5. **启动抢票**（建议 08:10 前）：到点自动开抢；命中即自动下单，需你在 30 分钟内支付；
6. **电脑保持唤醒 + 联网**；出现验证码/核验工具会停下转交本人。

> 提醒：各车站**起售时间不同**（如北京南为 12:45，部分车站 08:15），务必按你出发站的官方起售时间填写。
