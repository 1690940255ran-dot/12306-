# 当前能力矩阵

更新日期：2026-09-19。真实官方页面能力均经 P0 现场验证（见 docs/acceptance.md）。

| 能力 | 状态 |
| --- | --- |
| 离线模拟查询、筛选、单轮与持续监控任务 | 已实现 |
| 真实余票查询（官方 deep link 页面 + DOM 解析） | **已验证（P0）**；结构变化自动熔断 |
| 真实起售时间查询（官方起售页接口） | **已验证（P0）**；读取不到不推算 |
| 官方登录窗口（用户本人完成扫码/核验） | **已验证（P0）**；登录态以官方账户页判定 |
| 会话加密保存（DPAPI）与跨进程恢复 | **已验证（P0）**；默认关闭，`--remember` 启用 |
| 查询合并与公平轮转调度（模拟验证） | 已实现；同键间隔 ≥ 配置值（下限 30 秒）+ 正向抖动 |
| 任务状态机（含 WAITING_SALE/EXPIRED/BOOKING）与订单/候补状态机 | 已实现；终态不可复活 |
| 订单/候补服务（授权快照、预检、幂等落库、单次提交、核对、重启恢复） | 模拟全流程已验收 |
| 真实下单链路（确认页核对→选择乘车人/席别→页面金额复检→选座弹窗→单次提交→结果分类） | **已验证（K4 现场出单）**：2026-09-19 真实创建 C436 待支付订单，官方可见 |
| 同一购票目标订单互斥（普通/候补） | 已实现（数据库活跃唯一索引强制） |
| 授权范围校验（A17：日期/车次/席别/乘车人/金额/过期/任务版本=配置哈希） | 已实现；页面金额在提交时复检上限 |
| 起售三段提醒（前 10 分钟/前 1 分钟/到点） | 已实现；过期不回放、全错过汇总一次 |
| 通知 outbox（持久化、渠道去重、30/120/300 秒重试） | 已实现；本地日志 + Windows 桌面 toast 通道 |
| 重试/冷却/熔断（退避 30/60/120、429 半开翻倍、单键熔断 10 分钟） | 已实现（虚拟时钟验证） |
| SQLite v3 持久化与 v1/v2 原位迁移 | 已实现 |
| 重启恢复（状态/快照/通知去重保留，停止任务不复活，未决订单只核对） | 已实现 |
| JSONL 轮转日志（字段白名单）、单实例锁 | 已实现 |
| GUI（任务列表/新建/单轮查询/mock 监控/托盘通知） | 最小切片已实现 |
| 真实下单/候补提交/支付核对 | **未实现**；接口明确报错，绝不模拟成功 |
| 账号脱敏标识显示（账户页昵称选择器） | 未命中，待适配（不影响登录态判定） |
| 票价解析 | 未实现（不绑定未验证编码）；金额不可核对时不自动命中 |
| 邮件/Webhook 通知渠道 | 未实现 |

## 验收场景覆盖

模拟验收 A02—A05、A07—A13、A15—A17、A19 见 `docs/acceptance.md`；真实场景验收在用户控制下逐项进行（A01 登录标识、A06 核验暂停、A08/A14 现场验证部分完成）。

模拟车次 DEMO-* 均为虚构；真实查询仅访问官方页面，不提交任何订单。
