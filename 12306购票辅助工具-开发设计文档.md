# 12306 购票辅助工具：开发计划与详细设计

版本：v1.0 · 编写日期：2026-09-18 · 首发环境：Windows 10/11，单机、单账号

本文是后续实现的开发基线，当前交付为设计文档，尚未创建可运行程序。文中的目录、命令和接口均为拟实现约定。按照“先验证接入、再搭框架、再完成各项功能”的顺序开发，每阶段通过验收后进入下一阶段。

## 1. 产品目标与交付边界

工具帮助用户保存出行需求、查询车次、低频监测余票、接收起售提醒，并在条件满足时辅助完成普通订单或候补订单。工具不承诺抢票成功，也不提供官方队列之外的优先购票能力。

首版覆盖境内直达列车、普通成人票、一个登录账号。允许多个备选日期、车次和席别，但同一购票目标只允许一个订单流程执行。学生票、儿童票、优惠资质、跨境车次、联程购票、自动退改签、多账号和云端托管放入后续需求池，不能将其套用普通成人票流程。

### 1.1 三种执行模式

| 模式 | 行为 | 适用阶段 |
| --- | --- | --- |
| 演示模式 `mock` | 使用本地模拟数据，完整运行查询、状态机、失败恢复和通知；不访问真实购票系统 | 从框架阶段开始，默认模式 |
| 辅助模式 `assisted` | 使用验证可用的官方页面流程查询，命中后展示购票摘要并打开官方页面，由用户确认提交 | 首个真实环境版本 |
| 自动提交模式 `auto_submit` | 用户事先授权具体购票目标；条件完全匹配时，通过已验证的页面流程提交一次订单，随后等待用户支付 | 通过真实接入与订单专项验收后开放 |

普通订单和候补订单分别设置自动提交开关，默认关闭。开启后按已授权条件执行，不重复要求用户确认相同任务；车次范围、乘车人、金额上限等关键条件发生变化时，原授权失效。自动提交不包括自动支付、自动取消订单或自动退款。

目前仅核查了公开官方页面，未验证登录后自动化查询、普通订单或候补订单流程，也未获得可作为本项目依赖的正式开放购票 API 文档。浏览器页面可访问不代表自动化接入已经获得官方许可或能够稳定运行。P0 必须确认适用平台规则和技术可行性；不满足条件的能力明确显示“不可用”，保留提醒和人工操作入口，不能以模拟成功代替真实完成。

### 1.2 已核实信息与需要动态确认的信息

官方登录页提供扫码和账号登录，并可能要求短信、滑动验证或 App 身份核验。因此设计采用官方页面登录，核验由用户本人完成。[官方登录页](https://kyfw.12306.cn/otn/resources/login.html)

候补需要相应人证核验；预付款完成后才进入有效候补，兑现结果应以官方订单记录为准。候补支付期限和可选兑现截止时间按当前页面读取。[候补购票说明](https://kyfw.12306.cn/otn/gonggao/alternate.html)

起售查询应关联日期和车站，不能假设全国车次统一起售。官网也提示起售、止售时间可能调整。[官方起售时间查询](https://www.12306.cn/index/view/infos/sale_time.html)

预售天数、可添加候补组合数、候补订单数、营业时间、付款时限、可选席别和优惠规则均视为可变规则。不同年代的官方说明也可能不一致，不能从旧文档复制固定限制。真实页面与旧配置冲突时暂停相关操作，并要求更新规则或人工处理。

## 2. 总体开发计划

以下为单名开发者的工作量估计，合计约 20—31 个工作日，不包含等待外部接入条件和规则变更的时间。

| 阶段 | 工作量 | 主要目标与产物 | 进入下一阶段的标准 |
| --- | --- | --- | --- |
| P0 接入验证 | 2—3 天 | 验证当前规则、登录、查询、起售、订单及候补页面；输出能力矩阵、风险记录和页面样本 | 每项能力标明已验证/受阻/不支持；确认可用的查询路径，未通过的写入能力禁止开启 |
| P1 项目框架 | 2—3 天 | 包结构、配置、领域模型、SQLite 迁移、日志、CLI、模拟适配器、单实例锁 | 离线演示可安装运行，能完成模拟查询→命中→订单待支付→模拟成功 |
| P2 查询与监控 | 3—4 天 | 车站及日期校验、车次筛选、查询合并、限频调度、起售提醒 | 模拟验收全部通过；真实查询仅在 P0 通过后开展人工控制的低频验收 |
| P3 登录与桌面界面 | 3—4 天 | 官方浏览器登录、会话管理、任务列表、查询页、详情页、托盘通知 | 登录失效和身份核验会暂停任务，界面不卡顿，能恢复受支持的只读任务 |
| P4 普通订单 | 3—5 天 | 授权快照、预检、单次提交、订单核对、支付提醒、重启恢复 | 模拟故障场景全部通过；真实自动提交只有在接入验证与实际人工购票验收完成后开放 |
| P5 候补订单 | 3—5 天 | 组合校验、候补预付款上限、提交、待支付与待兑现区分、兑现追踪 | 模拟候补全流程通过；真实功能受相同接入门槛控制 |
| P6 发布与稳定性 | 4—7 天 | 安装包、数据清理、故障诊断、用户说明、长时间模拟运行 | 在干净 Windows 环境安装启动；无重复下单、敏感信息泄露和未解决的高优先级故障 |

阶段交付采用独立版本：P1 为离线原型；P2—P3 为查询提醒 MVP；P4—P5 为订单辅助版；P6 为正式桌面版。如果真实写入能力受阻，应交付已验收的查询提醒版，并明确列出缺失功能。

## 3. 技术架构与运行方式

### 3.1 技术选型

- Python 3.12：统一领域逻辑和命令行；依赖兼容性在 P1 验证并生成锁文件。
- PySide6：Windows 桌面界面、托盘、后台任务进度。
- Typer：CLI 命令入口，供调试和高级用户使用。
- Playwright：在有界面的独立浏览器中处理经过验证的官方页面流程。
- Pydantic：配置、接口输入和领域边界校验。
- SQLite：保存任务、状态迁移、订单尝试、通知和恢复信息；通过迁移脚本管理结构。
- Python logging：结构化 JSONL 日志；pytest：核心状态机、限频与恢复测试。
- PyInstaller：在 P6 验证打包；浏览器运行时作为明确的安装依赖单独处理。

不预先绑定未验证的私有接口路径、请求参数、签名算法或 Cookie 字段。若以后取得正式授权接口，新增适配器替换页面实现，领域层无需变化。

### 3.2 分层原则

```text
桌面 GUI / CLI
       ↓ 用户命令、状态订阅
Application：任务服务、订单协调、会话协调
       ↓
Domain：筛选规则、状态机、授权校验、限频策略
       ↓ 抽象端口
Adapters：模拟服务 / 官方浏览器页面 / 未来授权接口
Infrastructure：数据库、操作系统凭据、通知、时钟、日志
```

GUI 只发命令和展示状态，不直接驱动页面。一个后台工作线程拥有独立 asyncio 事件循环及全部浏览器对象，数据库写入通过统一存储层串行化。通过 Qt 信号返回结果，不跨线程共享页面句柄。

首版单进程运行，GUI 与 CLI 共用引擎和数据目录，不允许两者同时启动监控引擎。第二个实例显示明确提示后退出；不自行另起浏览器会话。后续若需要远程控制，再增加经过认证的本机服务。

### 3.3 拟定命令

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e ".[dev]"
.\.venv\Scripts\python.exe -m playwright install chromium
.\.venv\Scripts\railassist.exe gui
.\.venv\Scripts\railassist.exe doctor
.\.venv\Scripts\railassist.exe query --from 北京南 --to 上海虹桥 --date 2026-09-25 --adapter mock
.\.venv\Scripts\railassist.exe task create --config config\task.example.yaml
.\.venv\Scripts\railassist.exe task run --id <任务ID> --adapter mock
.\.venv\Scripts\railassist.exe task list
.\.venv\Scripts\railassist.exe task pause --id <任务ID>
.\.venv\Scripts\railassist.exe task stop --id <任务ID>
```

命令为实现目标，目前不能执行；示例日期只是输入格式演示。正式发布后安装包提供桌面入口，不要求普通用户执行上述安装命令。

## 4. 项目目录与模块职责

```text
12306/
├─ pyproject.toml                 # 包定义、依赖、CLI 入口、质量工具配置
├─ requirements.lock             # 经验证的依赖锁定结果
├─ README.md                     # 安装、启动、限制、故障处理
├─ .gitignore                    # 排除运行数据、凭据、日志、浏览器数据
├─ config/
│  ├─ app.example.yaml
│  └─ task.example.yaml
├─ docs/
│  ├─ design.md                  # 本文作为后续版本基线
│  ├─ capability-matrix.md       # 接入能力、验证日期、依据
│  ├─ acceptance.md              # 分阶段验收记录
│  └─ runbook.md                 # 恢复、更新、人工接管
├─ src/railassist/
│  ├─ __init__.py
│  ├─ __main__.py
│  ├─ bootstrap.py              # 依赖装配、启动与退出
│  ├─ cli.py
│  ├─ config.py
│  ├─ domain/
│  │  ├─ models.py              # 查询、余票、任务、订单、候补模型
│  │  ├─ states.py              # 合法状态及迁移
│  │  ├─ matching.py            # 车次、席别、人数、价格匹配
│  │  ├─ authorization.py       # 自动提交授权范围校验
│  │  └─ errors.py              # 统一错误分类
│  ├─ ports/
│  │  ├─ railway.py             # 铁路服务接口
│  │  ├─ repository.py
│  │  ├─ notifier.py
│  │  └─ clock.py
│  ├─ application/
│  │  ├─ task_service.py
│  │  ├─ scheduler.py           # 公平队列、查询合并、截止调度
│  │  ├─ session_service.py
│  │  ├─ booking_service.py     # 提交与订单核对协调
│  │  ├─ waitlist_service.py
│  │  └─ recovery_service.py
│  ├─ adapters/
│  │  ├─ mock.py
│  │  └─ browser/
│  │     ├─ adapter.py
│  │     ├─ session.py
│  │     └─ pages/              # login/query/order/waitlist 页面对象
│  ├─ infrastructure/
│  │  ├─ database.py
│  │  ├─ migrations/
│  │  ├─ rate_limit.py
│  │  ├─ secret_store.py
│  │  ├─ notifications.py
│  │  ├─ logging_setup.py
│  │  └─ instance_lock.py
│  └─ ui/
│     ├─ main_window.py
│     ├─ task_editor.py
│     ├─ query_view.py
│     ├─ order_view.py
│     └─ worker.py
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  ├─ recovery/
│  └─ fixtures/                 # 脱敏页面片段、模拟时钟和故障场景
└─ scripts/
   └─ build.ps1
```

运行数据统一存放于 `%LOCALAPPDATA%\RailAssist\`，包含 `app.db`、`logs\`、`cache\`。登录默认使用不持久化的浏览器上下文；可选保存会话必须通过操作系统保护加密，不能直接将 Playwright 明文 storage_state 写入仓库或普通配置文件。

## 5. 核心功能详细规格

### 5.1 车次、日期与车站查询

输入字段：出发站、到达站、乘车日期集合、车次白名单/类型、出发时段、席别优先级、乘客数量、总金额上限。日期按出发站当地日历解释，境内首版统一 `Asia/Shanghai`。

车站保存正式名称和站点代码；“城市”与“车站”分开建模。同城站扩展默认关闭，启用时必须展示最终车站列表。出发日期是所选上车站的发车日期，展示跨日到达标记和总历时，不用车次始发日替代上车日期。

输出字段：车次、发到站、发到时间、历时、席别、票价（若可读取）、余票状态、候补可用性、起售信息、采集时间和来源。

余票规范化为 `AVAILABLE / COUNT(n) / SOLD_OUT / WAITLIST_ONLY / NOT_ON_SALE / UNKNOWN`。空白、解析失败、限流提示不能转换成“无票”；显示“数据不可用”并记录错误。模糊的“有票”不表示多人票量必然充足，订单预检仍需确认。

一份区间和日期查询结果在内存中筛选多个车次，不能为每个席别分别发起重复请求。查询仅是快照，不保证随后可预订。

### 5.2 余票监控与备选策略

任务保存明确的候选组合及排序。默认依次按“用户指定日期顺序→车次顺序→席别顺序”选择，允许用户切换为出发时间或价格排序；相同结果采用稳定排序。

首次命中后通知；只有无票变有票、候选变化或订单状态变化时再次通知。无座、不同日期、不同站点均不得隐式加入备选。用户设置“全部同行者同时购买”，本地不能自动拆成多人分单。

设置开始时间和停止时间；停止时间不得晚于所选车次适用的止售截止。连续无票属于正常轮询，不属于失败重试。监控不能无限运行，任务到期后进入 `EXPIRED`。

同一出行目标的普通订单与候补订单互斥：候补预付款生效后，默认停止该目标的自动普通购票；仍可只读监测并提示新选择。切换路线由用户处理既有订单，避免候补和普通订单同时兑现或生成重复行程。

### 5.3 起售提醒

从已经验证的官方查询页面获取起售时间，保存来源、查询时间和可信状态；也支持用户手动输入，并标记“手动设置”。无法确定时不推算成已确认时间，不开启依赖该时间的自动提交。

默认在起售前 10 分钟、前 1 分钟和到点时提醒。提醒使用本地定时器，不需要为倒计时反复访问官网。到点只向统一调度器申请一次查询，不突破限频。

启动时检查本机时区并展示系统时间；发现时间跳变后重新计算调度，不回放已过期的密集提醒。重启时距事件超过 5 分钟的旧提醒只汇总一次。

### 5.4 自动提交普通订单

执行前必须满足：接入能力已验证、任务自动提交已授权、登录有效、乘车人核验有效、当前在售、候选完全匹配、金额不超过上限、没有关联待支付/状态不明订单、任务未暂停或到期。

提交流程：

1. 获取账号级订单锁及购票目标锁，防止多个候选同时提交。
2. 从官方页面重新确认实际区间、日期、车次、席别、乘车人及总金额；无法读取关键字段则转人工。
3. 在本地事务中持久化订单尝试及 `SUBMITTING` 状态，再执行一次页面提交。
4. 读取并区分排队、待支付、拒绝和身份核验等结果；点击成功不代表订单成功。
5. 出现待支付订单时停止同目标提交，展示官方付款截止时间和入口，提醒用户支付。
6. 仅在官方订单记录明确显示支付成功/出票成功后标记 `FULFILLED`。付款页面跳转和本地通知均不能作为成功依据。

用户可在任务详情直接查看授权摘要。授权记录包括任务版本、乘车人引用、候选范围、最高总金额、是否接受无座、有效期限、允许的普通/候补动作及确认时间。

### 5.5 候补下单

候补是独立订单流程，不能把普通订单失败简单转换为候补成功。候补组合必须来自用户允许的日期、车次和席别；是否接受无座或新增列车必须显式设置，默认关闭。

步骤：读取当前候补资格和限制→形成候补方案→核对乘车人、组合、预付款及兑现截止→校验单独候补授权→提交一次→等待用户预付款→确认官方已生效→低频跟踪兑现状态。

`WAITLIST_PENDING_PAYMENT` 与 `WAITLIST_ACTIVE` 必须分开：提交了候补订单但未付预付款，不能显示“正在候补”。候补金额与普通票价使用各自上限；预付款不明或超限立即转人工。

生效后不重复提交同一目标的候补单。到期、兑现、失败和主动终止分别展示；退款状态独立追踪，不把“候补失败”显示成“退款到账”。取消或修改候补通过人工官方入口进行，首版不自动操作。

## 6. 登录与身份验证

点击“登录 12306”打开工具专用的官方浏览器窗口，首选扫码登录。用户也可在官方页面直接输入账号密码，程序不提供代存密码表单。

登录状态为 `LOGGED_OUT / LOGIN_PENDING / AUTHENTICATED / VERIFICATION_REQUIRED / EXPIRED`。通过已验证的账户页面标记判断登录，不以 Cookie 存在或页面标题判断。账号更换后，旧任务的乘车人映射和授权全部失效。

验证码、短信、人脸及 App 核验均在官方界面由用户完成；不接入打码平台、不绕过核验、不自动重复发送短信。出现验证请求后暂停相关账号的自动页面操作，通知用户；完成后重验登录和任务条件，再恢复。

遇到登录失效：只读任务进入等待登录；正在提交的订单先标记结果不明，重新登录后优先核对订单，不能直接重放提交。无法自动读取乘车人或资质时，保留官方页面人工选择流程并禁用该任务的自动提交。

首版默认关闭持久会话。开启“记住登录”时，通过 Windows DPAPI 或系统凭据设施保护会话数据，说明同一 Windows 用户下的进程仍可能访问解密后的数据。退出登录时清理内存及本地保存会话。完整身份证、密码、验证码、Cookie 不写日志；界面仅显示必要的脱敏识别信息。

## 7. 查询频率、请求控制与任务调度

以下是本工具保守的初始参数，不是 12306 公布的配额，也不能保证不会触发平台限制。平台明确限制、服务器提示和当前规则始终优先。

| 场景 | 默认策略 |
| --- | --- |
| 普通余票监测 | 同一查询键间隔 60 秒，并增加 0—20% 正向抖动 |
| 可配置下限 | 同一查询键不低于 30 秒；必须同时满足全局预算 |
| 全局调度 | 工具发起的业务操作并发 1，启动间隔至少 15 秒，突发容量 1 |
| 普通待支付/排队订单核对 | 默认 30 秒；官方页面有指定间隔则取更慢值 |
| 已生效候补状态核对 | 默认 300 秒 |
| 登录状态确认 | 复用正常页面反馈；没有业务活动时不专门反复请求 |
| 起售前 | 缓存起售时间；不通过高频查询模拟倒计时 |
| 非服务时段 | 停止不支持的业务；已生效候补的核对按可用能力处理 |

“业务操作”是一次查询、导航或订单核对，不等于浏览器全部 HTTP 子请求数量。页面自动刷新及后台请求需要在 P0 检查，不能宣称本地操作限频就等同于所有网络请求限频；发现不可控高频行为应关闭相关页面并暂停功能。

查询键包含出发站、到达站、乘车日期和影响返回结果的票种等参数。相同查询键的多个任务共享一次采集结果；不同查询键公平轮转。4 个查询键至少需要约 60 秒完成一轮，更多查询键会增加实际间隔，界面显示预计下一次查询，不虚报每项都能达到配置间隔。

订单核对优先于新余票查询，但所有操作共享账号和进程预算。单次订单提交不参与通用查询重试。收到停止命令后取消尚未发出的请求，已发出的提交继续核对结果，不能把本地停止当作远端取消。

## 8. 异常处理、退避与失败恢复

| 错误类别 | 处理规则 |
| --- | --- |
| 查询网络超时、临时 5xx | 同一读取操作最多 3 次重试；指数退避 30、60、120 秒，加 0—20% 正向抖动；仍受全局限频 |
| 请求频繁、429 | 遵守可读取的 `Retry-After`；否则至少冷却 5 分钟；半开只允许一次只读探测，再失败则冷却翻倍、上限 30 分钟 |
| 403、风控或验证页面 | 暂停账号自动操作并通知人工处理，不自动尝试绕过 |
| 登录失效 | 等待用户登录，不循环重试账号密码 |
| 无票/未开售 | 正常调度等待，不计入网络错误计数 |
| 金额超限、乘车人无效、行程冲突 | 停止本次下单并说明原因；配置修正后才能恢复 |
| 页面字段/选择器失效 | 能力熔断为不可用；保存脱敏诊断，不按坐标猜测提交 |
| 提交超时或断连 | 标记 `OUTCOME_UNKNOWN`，优先查询官方订单，禁止盲目重提 |
| 数据库写入失败 | 暂停所有写入性动作，不能在没有持久记录时继续下单 |
| 通知失败 | 独立重试并显示本地未送达标记，不重新执行购票动作 |

只读操作默认总超时 30 秒、导航超时 45 秒；实际值由适配器测试调整。单查询键连续 5 次读取失败熔断 10 分钟；账号级风控覆盖所有任务。每次恢复最多一个探测，不能同时唤醒全部积压任务。

提交结果不明时可按 30、60、120 秒核对三次；仍无法确认则进入人工处理并保留购票目标锁。核对必须覆盖普通未支付订单、已完成订单及候补订单相关状态。不能仅凭“未支付列表没找到”断定没有订单。

本地幂等键由账号引用、购票目标、任务版本及动作类型构成，数据库对同一活动订单尝试设置唯一约束。此措施只能防止本机重复执行，不能让外部服务具有原子性或恰好一次语义。因此必须采用“提交前落库＋结果不明先核对”的组合。

进程重启后加载未终结任务，先检查锁与会话，再核对 `SUBMITTING / OUTCOME_UNKNOWN / PENDING_PAYMENT / WAITLIST_ACTIVE`。只读监控可按用户设置恢复；未决提交禁止自动重放。

## 9. 数据模型、状态机与接口

### 9.1 主要模型及存储表

| 模型/表 | 关键字段 |
| --- | --- |
| `tasks` | id、revision、goal_id、account_ref、查询条件、优先级、start_at、stop_at、status、next_run_at |
| `authorizations` | task_id、revision、动作范围、乘车人引用、金额上限、expires_at、confirmed_at |
| `ticket_snapshots` | query_key、train、seat、availability、price_fen、observed_at、source、validity |
| `order_attempts` | id、goal_id、idempotency_key、action、status、remote_order_ref、created_at、last_checked_at |
| `waitlist_orders` | attempt_id、组合引用、prepayment_fen、deadline、status、refund_status |
| `state_events` | event_id、entity_id、from_state、to_state、reason_code、occurred_at |
| `notification_outbox` | event_id、channel、status、retry_count、next_attempt_at、delivered_at |
| `capabilities` | adapter_version、能力名称、验证结果、verified_at、规则来源 |

金额用整数“分”保存，禁止浮点金额比较；时间戳保存 UTC、显示上海时间，乘车日期单独保存本地日期。`observed_at` 表示实际采集时间，读取缓存不更新它。订单前关键确认必须来自当前页面，不能直接依据过期余票快照提交。

乘车人只保留最小必要的本地引用、脱敏名称和校验状态；敏感原始数据默认不落库。SQLite 本身不是加密存储，敏感字段若确需保存应单独加密，密钥不放入同一个数据库。

### 9.2 分离任务状态与订单状态

```text
任务：DRAFT → READY → WAITING_SALE / MONITORING
      MONITORING → MATCHED → BOOKING → COMPLETED
      活动状态 → PAUSED / NEEDS_USER_ACTION / EXPIRED / STOPPED / FAILED

普通订单：PREPARED → SUBMITTING → QUEUED → PENDING_PAYMENT → FULFILLED
          SUBMITTING / QUEUED → OUTCOME_UNKNOWN → RECONCILING
          RECONCILING → QUEUED / PENDING_PAYMENT / FULFILLED / NEEDS_USER_ACTION
          明确拒绝 → REJECTED；官方确认取消/超时 → CANCELLED / EXPIRED

候补订单：PREPARED → SUBMITTING → WAITLIST_PENDING_PAYMENT
          → WAITLIST_ACTIVE → FULFILLED / UNFULFILLED / CANCELLED
          SUBMITTING → OUTCOME_UNKNOWN → RECONCILING
```

示意箭头不能替代完整迁移表：实现时在 `states.py` 逐一列出允许的事件、前置条件和副作用。终态不接受旧事件回退；按任务版本和事件 ID 去重，状态更新与通知 outbox 写入同一数据库事务。`COMPLETED` 仅代表目标已从官方订单确认完成，不由“已发通知”触发。

### 9.3 适配器接口约定

```python
class RailwayPort(Protocol):
    async def capabilities(self) -> CapabilitySet: ...
    async def session_status(self) -> SessionStatus: ...
    async def open_login(self) -> UserActionRequired: ...
    async def query_tickets(self, query: QuerySpec) -> TicketSnapshot: ...
    async def query_sale_time(self, query: SaleTimeQuery) -> SaleTimeResult: ...
    async def prepare_order(self, intent: BookingIntent) -> PreparedOrder: ...
    async def submit_order(self, prepared: PreparedOrder) -> SubmissionResult: ...
    async def prepare_waitlist(self, intent: WaitlistIntent) -> PreparedWaitlist: ...
    async def submit_waitlist(self, prepared: PreparedWaitlist) -> SubmissionResult: ...
    async def reconcile(self, attempt: OrderAttempt) -> ReconcileResult: ...
    async def close(self) -> None: ...
```

上述为签名草案，类型在领域模块实现。`PreparedOrder` 包含当前已核对摘要、页面上下文标识和短时有效期；页面或任务改变则失效。`SubmissionResult` 必须区分明确接受、明确拒绝、排队、需人工和结果不明；不能仅返回布尔值。

`CapabilitySet` 至少包含查询、起售查询、普通提交、候补提交和订单核对。下单与对应核对能力必须同时可用。所有适配器访问统一经过调度与预算层，页面内部不能自行启动无限轮询。

## 10. 配置约定

```yaml
schema_version: 1
adapter: mock
mode: assisted
timezone: Asia/Shanghai

query:
  from_station: 北京南
  to_station: 上海虹桥
  dates: ["2026-09-25"]
  train_codes: []              # 空列表为该区间全部车次；自动提交前须明确确认范围
  seat_priority: [二等座]
  allow_same_city_stations: false
  allow_no_seat: false

passengers:
  refs: ["passenger-demo-1"]   # 演示引用；真实引用从本人账号中选择

purchase:
  auto_submit: false
  max_total_amount_fen: 80000
  require_all_passengers: true

waitlist:
  enabled: false
  auto_submit: false
  max_prepayment_fen: 80000
  accept_added_trains: false
  deadline: null              # 启用前必须选择官方允许的截止时间

monitor:
  interval_seconds: 60
  positive_jitter_ratio: 0.2
  start_at: "2026-09-18T09:00:00+08:00"
  stop_at: "2026-09-24T22:00:00+08:00"

notifications:
  desktop: true
  sound: true
  email: false
  webhook: false
```

此配置用于展示结构，真实任务须更新日期、乘车人及金额并通过校验。不能通过 YAML 中一个布尔值绕过授权：自动提交还需要与任务版本匹配的授权记录和可用能力。通知密钥使用凭据存储引用，不写在 YAML。

配置校验包括：日期非过去日期、站点不同、日期在当前可售范围内或明确设为等待起售、乘客数非零、优先级无重复、开始早于停止、金额为正、候补截止合法。导入配置只创建草稿，不自动触发真实下单。

## 11. 界面与用户操作流程

桌面主窗口使用“任务列表＋任务详情＋底部状态栏”：列表显示区间、日期、模式、最新余票、上次及下次查询、任务状态；详情显示备选、授权摘要、订单和事件时间线；状态栏显示账号、接入能力、网络、限频冷却和通知状态。

新建任务流程为：选择日期与车站→设置车次和席别→选择乘车人→设置预算与候补偏好→预览任务→保存或启动。开启自动提交时，直接展示完整授权摘要并记录确认，不隐藏于全局设置。

提供“立即查询”“暂停”“停止”“打开官方页面”“重新登录”“清除本地数据”。立即查询仍走统一限频队列。暂停停止新的自动动作，但对已提交订单保留必要状态核对；停止不取消远端订单，界面清楚显示已有订单状态。

关闭窗口默认弹出“退出程序 / 最小化到托盘”选项，可记住选择；退出前持久化状态。托盘运行要求电脑保持联网且未休眠，不承诺退出或关机后的持续监控。唤醒后先恢复会话、核对未决订单，再恢复查询，不补发休眠期间积压操作。

## 12. 日志与通知

日志采用 JSONL，至少记录时间、级别、task_id、attempt_id、event_id、动作、耗时、错误分类和恢复建议。日志按 10 MB 轮转，默认保留最多 10 份且不超过 7 天；诊断包只导出脱敏配置、版本、能力状态和相关错误。

记录“查询完成、状态变化、限频、登录失效、下单开始、订单核对、待支付、候补生效、兑现、通知失败”。不记录密码、验证码、完整身份证、完整手机号、会话令牌、付款凭据或含敏感查询参数的完整 URL。真实页面截图默认关闭，用户主动开启诊断时仍要脱敏。

首版支持 Windows 桌面通知、提示音和程序内事件中心。扩展渠道为用户自配 SMTP 邮件或 HTTPS Webhook；必须显式启用并测试，网络凭据安全保存，不默认外发个人行程。

起售提醒、余票命中、需要核验、待支付、支付即将到期、候补生效、兑现和异常中断属于通知事件。订单待支付按官方截止时间调度；不硬编码统一付款时长。桌面通知无法保证用户看见，程序内保留未读状态和最后送达结果。

通知使用 outbox 持久化，按事件和渠道去重，失败后在 30、120、300 秒重试三次。远端渠道超时可能已收到消息，因此采用至少一次投递语义，支持幂等键的渠道传递 event_id。通知失败不会触发再次下单。

## 13. 测试与验收清单

自动化测试只使用模拟适配器、脱敏页面 fixture、本地测试页面和虚拟时钟，不对真实 12306 做压力测试，也不在 CI 自动登录或下单。真实验收由用户在真实购票需求下控制执行，未验证的能力不得标成已交付。

| 编号 | 验收场景 | 预期结果 |
| --- | --- | --- |
| A01 | 安装后无任何账号或密钥 | mock 模式可完整演示；真实功能清楚标识未登录/未验证 |
| A02 | 同日期区间创建多个筛选任务 | 合并查询，不按车次和席别重复访问 |
| A03 | 多个日期及车站组合 | 公平调度，实际间隔不低于预算约束 |
| A04 | 余票从无变有、多人票量不足 | 正确通知；票量不确定时不承诺全部可购 |
| A05 | 起售提醒、休眠、系统时间跳变 | 提醒去重，不补发密集查询 |
| A06 | 登录失效、短信或滑动核验 | 暂停账号自动操作，人工核验后重验条件 |
| A07 | 429、5xx、网络断开 | 按分类退避，展示冷却，无重试风暴 |
| A08 | 提交已到服务器但客户端超时 | 标记不明，核对后恢复，绝不直接重复提交 |
| A09 | 两任务同时命中同一目标 | 仅一个获取锁并提交；另一个等待或结束 |
| A10 | 提交前、提交后、落库前后进程崩溃 | 重启先恢复与核对，不重放未决写操作 |
| A11 | 普通订单待支付及支付到期 | 不显示已出票，使用真实截止时间提醒 |
| A12 | 候补已提交但未付预付款 | 显示待支付，不能显示有效候补 |
| A13 | 候补已生效且普通余票出现 | 只提示，默认不再次自动买入 |
| A14 | 页面结构改变或金额读不到 | 关闭相关自动能力，转人工，不误点击 |
| A15 | 停止、退出和重新启动 | 已发提交继续按未决处理，不擅自取消订单 |
| A16 | 通知不可达或超时 | 独立重试和去重，不改变购票结果 |
| A17 | 新任务超出旧授权范围 | 拒绝自动提交，要求重新确认授权摘要 |
| A18 | 日志及诊断包检查 | 不出现密码、验证码、身份证及会话令牌 |
| A19 | 24 小时模拟运行及数据库迁移 | 任务可恢复，无无限队列、重复提交或迁移数据丢失 |

P6 发布阻断条件：任一重复提交风险、错误宣称出票、绕过授权范围、身份数据泄露、限流不生效、订单不明时继续提交。性能目标为界面操作反馈通常小于 200 ms、空闲监控不阻塞 GUI；实际内存与启动时间在打包测试中记录，不以浏览器进程数之外的估算代替测量。

## 14. 后续实施顺序与完成定义

第一批先完成 P0 能力清单、P1 框架和 mock 演示，不将真实订单写入作为初始化脚手架的副作用。开发提交按以下顺序拆分：

1. 初始化包、依赖锁、文档、配置校验和领域类型。
2. 实现状态机、SQLite 迁移、日志脱敏、单实例锁。
3. 实现模拟适配器、CLI 查询及任务演示。
4. 实现限频调度、合并查询、提醒和通知 outbox。
5. 接入 P0 已验证的真实查询页面及官方登录窗口。
6. 加入桌面界面和恢复流程。
7. 实现普通订单预检、授权与核对，先通过故障模拟再开放真实能力。
8. 实现候补流程及其与普通订单的互斥规则。
9. 执行分阶段验收、打包、安装验证并补齐用户手册。

每个阶段交付可运行产物、必要验证结果、使用说明和已知限制。测试通过只证明已覆盖场景；只有真实能力验收记录存在且仍与当前页面相符时，才能将相关功能标注为“支持”。

这份方案足以指导项目结构、业务模块和可靠性机制的实现；真实 12306 接入仍必须通过 P0 的当前页面验证来补齐，不能仅凭本文保证无人值守购票全流程可用。
